## 调用方式

点击显示歌词

```js
import Icon from '@/components/Icon';
import PopSelect from '@/components/PopSelect';

<PopSelect title="歌词查看">
  <Icon onClick={() => '弹窗的显示内容'} icon="icon-quku_qupu" className="lib-song-action__item" />
</PopSelect>;
```
